<?xml version="1.0" encoding="utf-8" ?>
<local:PageBase
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:BrickController2.UI.Controls"
    xmlns:converters="clr-namespace:BrickController2.UI.Converters"
    xmlns:extensions="clr-namespace:BrickController2.UI.MarkupExtensions"
    xmlns:ios="clr-namespace:Microsoft.Maui.Controls.PlatformConfiguration.iOSSpecific;assembly=Microsoft.Maui.Controls"
    xmlns:local="clr-namespace:BrickController2.UI.Pages"
    x:Class="BrickController2.UI.Pages.SettingsPage"
    Title="{extensions:Translate Settings}"
    ios:Page.UseSafeArea="True"
    BackgroundColor="{DynamicResource PageBackgroundColor}">

    <local:PageBase.Resources>
        <ResourceDictionary>
            <converters:ThemeStyleToBoolConverter x:Key="ThemeTypeToBool"/>
        </ResourceDictionary>
    </local:PageBase.Resources>

    <local:PageBase.Content>
        <AbsoluteLayout HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
            <Grid RowSpacing="0" AbsoluteLayout.LayoutBounds="0, 0, 1, 1" AbsoluteLayout.LayoutFlags="All">

                <!-- Settings items -->
                <Grid Margin="20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/> <!-- Theme -->
                        <RowDefinition Height="Auto"/> <!-- Divider -->
                        <RowDefinition Height="Auto"/> <!-- Language -->
                        <RowDefinition Height="Auto"/> <!-- Divider -->
                        <RowDefinition Height="Auto"/> <!-- McpServer -->
                        <RowDefinition Height="Auto"/> <!-- McpServerPort -->
                        <RowDefinition Height="Auto"/> <!-- McpServerAuthToken -->
                        <RowDefinition Height="Auto"/> <!-- Divider -->
                    </Grid.RowDefinitions>
                    
                    <!-- Theme -->
                    <Label Grid.Column="0" Grid.Row="0" Text="{extensions:Translate Theme}" FontSize="Large" VerticalOptions="Center"/>
                    <Button Grid.Column="1" Grid.Row="0" Text="{Binding CurrentTheme}" Command="{Binding SelectThemeCommand}" Style="{StaticResource PickerButtonStyle}" HorizontalOptions="FillAndExpand"/>

                    <BoxView Grid.ColumnSpan="2" Grid.Row="1" Style="{StaticResource DividerBoxViewStyle}" Margin="5,10,5,10"/>
                    <!-- Language -->
                    <Label Grid.Column="0" Grid.Row="2" Text="{extensions:Translate Language}" FontSize="Large" VerticalOptions="Center"/>
                    <Button Grid.Column="1" Grid.Row="2" Text="{Binding CurrentLanguage}" Command="{Binding SelectLanguageCommand}" Style="{StaticResource PickerButtonStyle}" HorizontalOptions="FillAndExpand"/>

                    <BoxView Grid.ColumnSpan="2" Grid.Row="3" Style="{StaticResource DividerBoxViewStyle}" Margin="5,10,5,10"/>                    
                    <!-- McpServer -->
                    <Label Grid.Column="0" Grid.Row="4" Text="{extensions:Translate McpServer}" FontSize="Large" VerticalOptions="Center"/>
                    <Switch Grid.Column="1" Grid.Row="4" IsToggled="{Binding CurrentMcpServerEnabled}" IsEnabled="{Binding McpServerIsAvailable}" HorizontalOptions="EndAndExpand"/>

                    <!-- McpServerPort -->
                    <Label Grid.Column="0" Grid.Row="5" Text="{extensions:Translate McpServerPort}" FontSize="Medium" VerticalOptions="Center" />
                    <Button Grid.Column="1" Grid.Row="5" Text="{Binding CurrentMcpServerPort}" Command="{Binding SelectMcpServerPortCommand}" Style="{StaticResource PickerButtonStyle}" HorizontalOptions="FillAndExpand"/>

                    <!-- McpServerAuthToken -->
                    <Label Grid.Column="0" Grid.Row="6" Text="{extensions:Translate McpServerAuthToken}" FontSize="Medium" VerticalOptions="Center" />
                    <Button Grid.Column="1" Grid.Row="6" Text="{Binding CurrentMcpServerAuthToken, Converter={StaticResource NoAuthTokenToPlaceholderConverter}}" Command="{Binding SelectMcpServerAuthTokenCommand}" Style="{StaticResource PickerButtonStyle}" HorizontalOptions="FillAndExpand"/>

                    <BoxView Grid.ColumnSpan="2" Grid.Row="7" Style="{StaticResource DividerBoxViewStyle}" Margin="5,10,5,10" />
                </Grid>

            </Grid>

            <controls:Dialogs x:Name="Dialogs" AbsoluteLayout.LayoutBounds="0, 0, 1, 1" AbsoluteLayout.LayoutFlags="All"/>

        </AbsoluteLayout>
    </local:PageBase.Content>
    
</local:PageBase>